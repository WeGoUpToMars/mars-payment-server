server:
  port: 18189

spring:
  profiles:
    include: pay

  datasource:
    url: jdbc:mysql://multipass:3333/mars?createDatabaseIfNotExist=true
    username: test
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: info
  com.jpa: debug
